/**
 * EXEMPLO: Como criar uma nova página no Next.js
 * 
 * Para criar a página "Sobre", faça:
 * 1. Crie a pasta: app/sobre/
 * 2. Crie o arquivo: app/sobre/page.tsx
 * 3. Use este código como base
 * 4. Acesse: http://localhost:3000/sobre
 */

import { Header } from "../../components/Header";
import { Footer } from "../../components/Footer";
import { Button } from "../../components/ui/button";
import { ArrowRight } from "lucide-react";

// Metadata da página (SEO)
export const metadata = {
  title: "Sobre - Sitara",
  description: "Conheça a história e missão da Sitara",
};

export default function SobrePage() {
  return (
    <div className="min-h-screen">
      <Header />
      
      <main>
        {/* Hero Section */}
        <section className="py-20 bg-gradient-to-br from-indigo-50 to-purple-50">
          <div className="container mx-auto px-4">
            <div className="max-w-3xl mx-auto text-center">
              <h1 className="mb-6">Sobre a Sitara</h1>
              <p className="text-lg text-foreground/70">
                Somos especialistas em análise de sistemas com mais de 10 anos
                de experiência ajudando empresas a transformar sua tecnologia.
              </p>
            </div>
          </div>
        </section>

        {/* Conteúdo */}
        <section className="py-20">
          <div className="container mx-auto px-4">
            <div className="grid md:grid-cols-2 gap-12">
              <div>
                <h2 className="mb-4">Nossa Missão</h2>
                <p className="text-foreground/70">
                  Transformar sistemas de software em vantagens competitivas
                  para nossos clientes através de análises profundas e
                  metodologias comprovadas.
                </p>
              </div>
              
              <div>
                <h2 className="mb-4">Nossa Visão</h2>
                <p className="text-foreground/70">
                  Ser referência em consultoria de análise de sistemas,
                  reconhecida pela excelência técnica e resultados entregues.
                </p>
              </div>
            </div>

            <div className="mt-12 text-center">
              <Button size="lg" className="gap-2">
                Fale Conosco
                <ArrowRight className="w-4 h-4" />
              </Button>
            </div>
          </div>
        </section>
      </main>
      
      <Footer />
    </div>
  );
}

/**
 * ROTAS DINÂMICAS - Exemplo Blog
 * 
 * Para criar: app/blog/[slug]/page.tsx
 */

/*
export default function BlogPostPage({ 
  params 
}: { 
  params: { slug: string } 
}) {
  return (
    <div>
      <h1>Post: {params.slug}</h1>
    </div>
  );
}
*/

/**
 * LOADING STATE - Exemplo
 * 
 * Criar: app/sobre/loading.tsx
 */

/*
export default function Loading() {
  return <div>Carregando...</div>;
}
*/

/**
 * ERROR HANDLING - Exemplo
 * 
 * Criar: app/sobre/error.tsx
 */

/*
'use client'

export default function Error({
  error,
  reset,
}: {
  error: Error;
  reset: () => void;
}) {
  return (
    <div>
      <h2>Algo deu errado!</h2>
      <button onClick={() => reset()}>Tentar novamente</button>
    </div>
  );
}
*/
